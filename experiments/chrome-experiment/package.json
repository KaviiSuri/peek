{
  "name": "peek-chrome-discovery-spike",
  "version": "0.0.0-experiment",
  "private": true,
  "type": "module",
  "description": "Throwaway MV3 action-popup test vehicle for Peek discovery; not production Peek.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "build": "node build.mjs",
    "verify:manifest": "node scripts/verify-manifest.mjs",
    "verify": "npm run typecheck && npm test && npm run build && npm run verify:manifest"
  },
  "dependencies": {
    "effect": "3.22.1"
  },
  "devDependencies": {
    "@types/chrome": "0.2.9",
    "@types/node": "26.4.1",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
