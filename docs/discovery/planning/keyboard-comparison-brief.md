# Peek keyboard-feel experiment

Continue Pi Opus 4.8 medium child. Read PEEK-5, resolved PEEK-8, and the GPT artifacts search-acceptance-note.md and browser-constraints-decision-note.md under /Users/kavii-suri/.bb/thread-storage/thr_gaza7yrmdr/. Parent owns product decisions and evidence checks, you own prototype edits. User wants continuous work and no review gates for Wayfinding; do not ask setup questions. Read frontend-design and prototype skills as needed.

Produce a new self-contained HTML DISCOVERY PROTOTYPE under your own BB_THREAD_STORAGE, preserving the previous layout comparison unchanged. Use chosen B layout, same synthetic 30-tab fixture, readable light/dark, meaningful URL beneath title, favicon placeholders labelled illustrative. No static shortcut legend inside Peek. No extension, frameworks, dependency installs, real tab access, backend, persistent state, stack choice or production code.

Question: does direct keyboard navigation during typing feel better than an explicit selection mode? Build two switchable experimental variants, external experiment controls clearly distinct from Peek. Direct: input remains ready, Up/Down highlight, Enter commits. Selection-mode: same input, Escape from typing enters selection mode (Escape again cancels); j/k or arrows navigate, 1–9 choose numbered visible results; i returns to typing. This is an explicit prototype hypothesis, NOT a selected keymap. Numbers type normally during search. Contextual mode hint/number badges allowed only when mode changes; no permanent generic footer. Handle browser composition and input focus sensibly without building a shortcut engine. Do not globally capture Cmd+K or other browser shortcuts. Launch with an external experiment button, not a fake claim of Chrome shortcut support.

Keep ranking constant across variants. Prefer a small deterministic precomputed query scenario set derived from acceptance examples rather than implementing a fuzzy matcher. If a simple fixture filter helps free play, explicitly label it approximate and NOT ranking validation. Search strings and timing in this toy do not prove extension speed. Supply 2–3 guided tasks outside palette using identical target/tab sets for both modes, plus reset/reopen. On commit, dismiss palette and show chosen synthetic tab in external feedback; actual Chrome page never changes. Highlight never commits. Cancel preserves simulated current tab. Maintain two distinct simulated states CURRENT and PREVIOUS: initially current is one fixture tab, previous is a different one; opening selects PREVIOUS, not current. After commit update previous to old current. The browser report phrase 'visible immediately before invocation' is ambiguous: don't interpret it as selecting current tab.

Record minimal per-trial explicit user actions and committed/canceled result outside palette if useful, no automatic judgment or claims of speed. Avoid huge telemetry UI. Ensure visible full input strings, keys do not leak to page scrolling, selection stays visible, long URLs/titles readable. Narrow screen supported without cramming both palettes simultaneously.

Scope only own thread storage; repo READ ONLY, broken git untouched, no commits required for external artifacts. No delegation. Verify via isolated agent_browser: launch/input focus, identical query/result data across modes, arrows don't mutate query or commit, typing numeric query works in search mode, selection mode numbers commit visible intended row, Escape mode transitions/cancel, reopen previous != current, light/dark, narrow view. Capture a few representative screenshots. Attach HTML and images to PEEK-5 and post one substantive verification comment with actual checks, limitations and gaps. Keep task in_progress; do not choose a mode, complete the decision, or ask Kavii directly. Return artifact paths and recommended first hands-on task to parent asynchronously. Use file:// if possible; if server necessary use bb terminal and share-server-links.
