# Peek result-layout discovery comparison

Continue as the Opus Pi child, medium reasoning, supporting parent thr_gdyshcku3c. Read PEEK-8 and resolved PEEK-2. User now explicitly wants us to continue doing preparation without procedural approval stops. No human review gates for Wayfinding; real product choices still his. Work on the visual comparison now, not more reference gathering.

Confirmed: Raycast-inspired visual finish/readability, recognisable favicons, prominent titles, quieter meaningful domain AND path (not just github.com), no static shortcut footer. Peek disappears on explicit tab selection; highlighting does not switch tab. Chrome-only, all windows within current profile. 25–30+ overlapping work tabs, especially GitHub issues/PRs/repos with similar titles, some Sheets. No cross-profile labels or features. No actual extension implementation, stack selection, production app scaffold or live tab access.

Question: which layout makes these overlapping results easiest to recognise? Produce THREE genuinely different row treatments using IDENTICAL synthetic realistic dataset and order, side by side at readable scale, light/dark comparison separately via page-level toggle. Suggested structural contrasts: A compact single row with trailing URL; B title plus second-line meaningful URL; C compact aligned title/source columns. Keep visual finish consistent so content hierarchy/density, not random colors, is the variable. Show about 8–10 results at a time with a scrollable 30-row fixture. Use realistic fictional GitHub names, no user/private data. URL truncation should preserve useful identity. Show selected row consistently. No instructional footer inside palettes. Clearly distinguish comparison controls from product UI. Include full-size single-variant mode for narrow screens, without making the main comparison unreadably tiny.

Read frontend-design and prototype skills. Adapt prototype/UI instructions to this agreed side-by-side comparison; existing project has no app, so do NOT install packages, pick React, or scaffold routes. Deliver one self-contained prototype HTML with embedded CSS/JS solely for viewing the alternatives, not a functional search implementation. No external dependencies/network tracking; inline local/simple recognizable favicon equivalents okay if labelled illustrative. Light/dark toggle and variant viewing controls must work. All product data is mocked; don't imply ranking, performance or shortcut behavior is implemented. Mark it a DISCOVERY PROTOTYPE.

Scope: write only in your own BB_THREAD_STORAGE, e.g. prototype-result-layouts.html and screenshots; repo READ ONLY, broken git pointer not yours to fix. Preserve artifacts through task attachments, no commits or merges required for external thread storage. No other agents. Use isolated agent_browser session; do not attach user browser. Open local HTML if supported or run isolated static server on an available port via bb terminal; use share-server-links skill if server needed. Verify rendered light and dark, identical row data/order across variants, no clipping at wide and narrow widths, comparison controls. Capture at least light+dark overview screenshots and representative full-size variant shots, visually inspect them. Attach HTML and screenshots to PEEK-8, one evidence comment with verification/gaps. Do not mark decision done or in_review; parent will bring actual choice to Kavii. Report paths and any preview server URL/terminal details. Finish the artifact without asking Kavii logistical questions.
