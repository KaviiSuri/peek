# Peek: consolidate technical recommendation and daily-use acceptance

Continue Pi GPT-5.6 Sol medium child thr_gaza7yrmdr under parent thr_gdyshcku3c. Read current PEEK-6, PEEK-7, PEEK-9, map PEEK-1 and your existing Poof, search and browser reports. User approved continuing the sequence: finish technical recommendation and acceptance checklist, then ONE narrow Chrome experiment, then first-release spec. No repeated abstract questionnaire. Do not implement an extension in this task.

User wants Effect TS akin to Poof, not its UI, and one excellent tab-finding capability. Parent recommendation is minimal Effect at browser boundaries, not copying Poof wholesale. Treat this as the preferred candidate, not a final measured stack claim. Consolidate a concrete small technical recommendation: which patterns to carry, which to omit, which current API/version facts need checking. If current upstream documentation is necessary to recommend an Effect version, verify through official sources with isolated agent_browser, keeping unverified facts explicit. Do not install packages, run builds, change Poof/Peek, or choose React/WXT/package manager by inertia. Recommend simple defaults within authority; elevate only meaningful unresolved risks. Do not create an artificial plain-TS-versus-Effect competition merely to reopen the user's stated preference. Effect size/readiness still needs measurement before claiming negligible impact.

Deliver TWO short Markdown artifacts under your own BB_THREAD_STORAGE:
1. technical-direction-recommendation.md: verified Poof lessons, recommended minimal Effect responsibilities, pure matching/interaction, Chrome boundary lifecycle, no unnecessary content/scan/settings machinery; precise pending empirical checks. Distinguish user confirmed vs owner recommended vs verified facts. Do not say final stack selected.
2. first-release-acceptance-draft.md for PEEK-7: consolidate already confirmed scope and expectations, plus explicit provisional owner defaults. Include B layout and meaningful URLs; light/dark; no static footer; Tab typing/selection preserving query/caret; j/k and visible number choices; arrows; Esc cancel; select-dismiss; no-op current selection preserves previous; strongest title+URL clues, fuzzy fragments, repo home case, recency ties; 25–30 repeated-site realistic tabs plus larger stress fixture. No guessed timing thresholds: define what must be measured (open-to-input-ready, keypress-to-results, comparing manual workflow, cold and warm) and tie readiness to user-observed speed/comfort. Include permission disclosure/current-profile scope, no content access, keyboard focus recovery, vanished tabs, unassigned shortcut state as sensible defaults not separately user-approved features. Real Chrome registration/focus/window/permissions are explicitly unverified gates; known duplicate/semantic misses remain accepted v0 limitations. Existing fixtures are hypothetical evidence, not passing production tests.

End acceptance draft with a tightly bounded Chrome experiment brief outline: WHAT to learn, minimal artifacts required, success observations, stopping condition, no production feature expansion. Explicitly avoid implementing full search or measuring a dummy prototype as final performance. Candidate presentation should be justified as a test vehicle, not final architecture. User loads extension manually; never install into or inspect personal profiles without permission. Plan one experiment with small adjustments, not many rival stacks. Scope remains no production code.

Work on PEEK-6 only as claimant; PEEK-7 is still dependency-blocked, so attach draft/comment there as preparatory input, not a completed decision. Attach tech artifact/comment to PEEK-6. No task status/map changes, no repo edits/git repairs/commits, no delegation, no user questions. Report asynchronously with recommendation and exactly what remains empirical. Parent owns adoption and any remaining product decision.
