# Peek narrow real-Chrome discovery experiment

Continue Pi GPT-5.6 Sol medium child. User approved sequence: technical/checklist consolidation -> ONE narrow Chrome experiment -> first-release spec. This authorizes preparing a throwaway test extension, NOT production Peek or installing/accessing user's browser. Read PEEK-9 and own technical-direction-recommendation.md + first-release-acceptance-draft.md. Read prototype, bb-orchestrate-threads and relevant testing skills as needed. Parent owns review/evidence and product decisions; you own edits. No human code-review gates for Wayfinding. No subdelegation.

FIRST fix acceptance draft's recurring ambiguity: previous = previously viewed DISTINCT tab BEFORE current, across window attention where observed, never current at invocation. Separate owner-derived examples/defaults from explicitly user-confirmed ones; do not label PR880 example/Shift+Tab details as individual user decisions. Update attachment and substantive note.

Prepare one unpacked MV3 action-popup experiment, a TEST VEHICLE not final architecture. Minimal Effect at browser seams, pure interaction. Verify current official Effect release/API facts before selecting a test dependency, use stable supported release unless concrete compatibility reason and disclose exact choice as experiment-only. No copying Poof RC by inertia. No React/WXT/app scaffold, no content scripts, host access, cloud services, production matcher, options/history/bookmarks/settings system. Popup may use plain DOM solely for test vehicle; this is not a production UI stack choice. Keep fixture/real-tab distinction clear: live tab titles/URLs in temporary profile, simple transparent filter if needed, never present it as completed fuzzy search or benchmark its ranking as final. Use confirmed B readability and Tab toggle/j-k/numbers/arrows/Enter/Esc, no effort on polish.

Read only tabs metadata needed; non-incognito current-profile normal windows; activate tab then window on explicit selection only; no-op current selection retains previous; cancelled highlight never activates. Respect MV3 synchronous listener registration, truthful limitations on recency after worker restart; don't invent history. Disclose storage permission if needed for session-only tracking rather than silently widening access; minimize permissions. Browser-scoped command starting Option+Space as TEST CANDIDATE, no global or Cmd+K interception. Provide Chrome remapping instructions for Cmd+Shift+Space and Cmd+Shift+E only if initial rejected; no automatic capture of other website shortcuts.

Scope files strictly under own BB_THREAD_STORAGE/chrome-experiment/. Repo and Poof READ ONLY, no repair of broken project git pointer. Preserve artifact history in an isolated git repo ONLY inside this new experiment directory, on a clearly named prototype branch if practical; never git-init or modify parent workspace. No commits/merges to Peek. Install dependencies ONLY inside experiment directory with pinned version/lockfile. Inspect third-party scripts before running, avoid unnecessary scripts. Save concise commands/results. Package self-contained unpacked build and ZIP for user to download, attach to PEEK-9. Include README: purpose, exact permissions, version, install/load steps for USER-CREATED temporary profile, fixture/test plan, cleanup/unload steps, supported/unsupported measured claims. Never install/load extension, access personal Chrome/CDP/profile, create external tabs or change settings on Kavii's behalf. When build ready, user must manually load and give comfort feedback.

Verify automatically everything possible WITHOUT personal browser: pure interaction/history regression tests, recorded/fake Chrome event wiring and two-step selection/cancel, manifest/bundle consistency, no unwanted permissions, build output size. Use fixture browser adapters for tests; don't claim they establish actual Chrome behavior. If supported isolated wrapper-owned extension browser exists, report capability but do not expand to personal profile or silently install. Do not spend time constructing automation for Chrome-only behavior user must observe. Instrument useful popup readiness/activation observations, but don't claim shortcut keypress-to-ready if browser action event timestamp is unavailable. Label cold/warm assumptions; no guessed performance thresholds.

Acceptance outline must remain narrow: command registration, focus, Tab navigation, explicit select/dismiss, two-window behavior, unassigned/closed-tab failure, no-op previous. Real webpage conflict matrix and user comfort remain pending manual evidence, not failed build. Stop at a verified usable experiment package, do not build production app. If blockers in dependencies/tooling occur, investigate narrow sensible fallback and report; no user questionnaire. Produce screenshots if isolated local popup fixture preview helps, clearly mock. Artifacts/report attached and one milestone comment, leave PEEK-9 in_progress. Report exact artifact paths, tests actually run, build size, experiment-only dependency choice, limitations, and smallest manual next step. Asynchronous, no waits/polling another thread.
