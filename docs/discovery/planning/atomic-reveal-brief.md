# Peek overlay: avoid empty-shell first-frame flash

Continue Pi Opus 4.8 medium child thr_673in7j8p5 as artifact editor. Parent owns evidence review/decisions. This is a narrow visual experiment defect, not resumption of stopped automated Dia investigation. DO NOT launch Dia, Chrome, attach profiles, install extensions, or alter user's loaded Downloads folders. No other agents.

Context: user confirmed v2 centered overlay keyboard-first now works in Dia, accepted Tab selection toggle, j/k and numbers, B layout. User reports split-frame hitch. Captured actual v3 export /Users/kavii-suri/Downloads/peek-v3-timeline (1).json (read only) shows two complete runs same worker, different renderer origins; host append precedes model response by about 14.8 and 10.9ms, focus another ~5ms later. These are not paint proof, no worker restart evidence, not a valid same-page idle pair. Asked whether empty box filling versus page freeze; user explicitly chose EMPTY BOX FILLING. Parent proposes single coherent reveal, not arbitrary delay/animation or runtime optimization.

Read actual v2/v3 source under sibling /Users/kavii-suri/.bb/thread-storage/thr_gaza7yrmdr/centered-overlay-experiment[-v3]/. Preserve them read-only. Prepare own NEW v4 experiment copy under own BB_THREAD_STORAGE/centered-overlay-reveal-v4, excluding node_modules/.git from copying. Dependencies may reuse via install with existing pinned lockfile in own directory; no repo/Poof edits or git repair. Preserve isolated git prototype history if practical, commit before report. V4 based on v2 behavior; v3 diagnostics may be retained if trivial but don't introduce more instrumentation/user capture chores.

Fix: don't visibly paint an empty shell while awaiting initial model. Construct/mount hidden without intercepting pointer/keyboard or focus; populate first model/results; atomically reveal complete panel then focus input. No arbitrary timeout. Valid zero-match/zero-tab state should reveal intentionally, not wait forever. Failure should have an honest recoverable error, not invisible stuck state. Pending cancel, repeat-toggle and navigation teardown must invalidate pending async completion so a dismissed shell cannot reveal later. Preserve toggle/one-host behavior, model stale-response guards, focus restoration only when appropriate, selected previous != current, same-current no-op history, Tab/caret/IME, permission set, no-popup _execute_action/MacCtrl+Space invocation. Do not change filter/ranking, shell geometry, accepted styling or feature scope.

Test red-before-green on actual mount/render path with delayed model promise: host not visible or focus-stealing before resolve, complete list visible with focus after resolve, delayed reject handled, cancel-before-resolve never reveals, repeat invocation no duplicates. Use existing DOM test dependencies if available; otherwise minimal isolated DOM fixture/browser tooling in HEADLESS agent-owned context only (no Dia and no external/site access). DO NOT assert correctness merely on a helper disconnected from production. Fake chrome bridge okay if testing actual bundled overlay DOM. If browser runner can't be safely headless, use DOM runner and explicitly report real paint unverified. No claim this eliminates user-visible flash until manual observation. Re-run existing unit/boundary/command parity and bundle checks, report exact tests, size, permissions, regression coverage. Attach zip, README-v4 and concise report to PEEK-9, status stays in_progress. Package preserved separately; return unpacked path for parent to verify/copy to NEW Downloads folder. No review gate, no user questions, report asynchronously.
