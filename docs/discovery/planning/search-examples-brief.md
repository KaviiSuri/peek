# Peek search decision preparation

Continue as Pi GPT-5.6 Sol medium, child of thr_gdyshcku3c. Read PEEK-3, map PEEK-1, and PRODUCT.md for context. The parent is the product conversation owner. User wants continuous progress, no procedural review gates for Wayfinding, no blocking waits. Prepare evidence/options, not product decisions or implementation.

Confirmed updates beyond PRODUCT.md: Kavii chose layout B (favicon, prominent title, quieter meaningful domain/path beneath) in PEEK-8. Strongest match FIRST is now explicitly confirmed. Recency as tie-breaker has NOT been confirmed. No static shortcut footer. Chrome-only invocation; all windows within current profile. Immediate default tab list, tab just left preselected; cross-window previous semantics remain open. 25–30+ overlapping work tabs, especially GitHub repos/issues/PRs; some Sheets. Users remember site, repo, title fragments or adjacent topic words. Title/URL only for v0; untitled duplicates need not be inferred from contents. Don't quietly add semantic search or content permissions.

Goal in user words: "if i had to think how to find something" he would abandon it. Parent was corrected for drifting into abstract questions between task results. Deliver a compact concrete comparison that makes ambiguous matching expectations easy to discuss ONE CASE AT A TIME here.

Read the synthetic 30-tab fixture in ../visual/prototype-result-layouts.html, READ ONLY. Reuse relevant examples so discussion stays grounded. Write a Markdown artifact with about 5–6 focused cases: repo query vs issue whose title contains same word; title fragment vs URL substring; typo/non-contiguous query; multiword query spanning repo/title; equally strong duplicates with recency as one option; adjacent-topic query absent from title/URL as a known limitation. For each: exact query, 3–5 candidate tab titles+URLs, clearly labelled illustrative last-used values only where relevant, two plausible orderings or responses if genuinely ambiguous, recommendation with rationale, and what remains the user's decision. Do not fabricate matching scores, measured performance, or claim there is an implemented matcher. Keep strongest-match-first settled; explore what strongest means through examples rather than reopening that decision. No algorithm/library selection, code, prototype, new implementation tickets or external research unless a specific claim needs checking. Avoid a huge questionnaire; lead with the highest-value first example, keep remaining examples available for progressive discussion.

Scope: write report only to your own BB_THREAD_STORAGE; repo and sibling artifacts READ ONLY. Broken git pointer is not yours to fix; no git commits for thread-storage research files. Parent will attach you and mark PEEK-3 in_progress. Attach artifact to PEEK-3 and post one substantive evidence/options comment. Do not resolve task, alter description/map, or ask Kavii directly. Verify that fixture-derived titles/URLs really exist in fixture and distinguish invented cases. Report concise result plus artifact path and recommended first case to parent. No delegation.
