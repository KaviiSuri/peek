# Peek centred overlay proving experiment

Continue Pi GPT-5.6 Sol medium child under parent thr_gdyshcku3c. User explicitly approved testing in-page overlay after disclosure: activeTab+scripting allow DOM execution/modification; some browser-owned pages unavailable; no persistent host grant or page-content search. Read PEEK-9 and your centred presentation memo. No further procedural questions. This is one narrow discovery test, not final architecture or production code.

Create NEW experiment directory under your own BB_THREAD_STORAGE (e.g. centered-overlay-experiment), leaving old chrome-experiment and Downloads installed folder untouched. Reuse tested pure interaction/previous logic where useful, pin stable known experiment dependencies. Separate isolated git history/prototype branch if practical; no git changes to Peek/Poof. No user browser/profile access, installs, global settings or actual tab inspection. No subdelegation. Parent handles evidence and user choices; you own artifact edits.

Build one unpacked MV3 extension with browser-scoped command for physical Control+Space on macOS (verify manifest uses MacCtrl mapping, not Ctrl->Command). This is Dia-tested user's candidate, NOT universal default. No action popup. On invocation inject a centred shell in active top-frame ordinary page via activeTab+scripting isolated world, with styling isolation. A minimal extension action click may invoke same behavior as a manual fallback for loading test, not open a popup. Exact permissions tabs, activeTab, scripting and storage ONLY if observed previous session storage retained; NO host_permissions, persistent content_scripts, native companion, remote assets or page text reads. Do not inject at startup or into all tabs. No unsafe HTML from tab titles/URLs. Real tab metadata list via worker is okay for select/window test; literal filter labelled test-only, not full fuzzy matcher. Preserve B layout, Tab toggles selection, j/k and numbers there, arrows and Enter, query/caret preservation, Esc always cancels, no static shortcut footer. Contextual selection hint only. No production polish or new features.

Overlay lifecycle: repeat invocation toggles/removes cleanly, one host only, remove event listeners with teardown. Escape/click-away cancel without tab switch, explicit commit removes overlay and activates selected tab then window, no-op current retains distinct previous. Restore pre-overlay page focus on cancel if still valid WITHOUT reading page content. Do not capture Cmd+K; respect composition and modified browser keys. Avoid overlay removal/restoration events polluting previous attention. Background listener registration synchronous. Lost/removed tabs must never cause wrong activation. Error on restricted documents must be visible honestly: e.g. clear action badge/title diagnostic and record exact scripting error; no silent framed-window fallback or new error tab. Keep user-facing explanation concise, no permanent page DOM on failed inject. Fullscreen/dialog/top-layer robustness is not promised by basic z-index; record limitations.

Verify everything possible automatically without personal browser access: typecheck, existing history/nav tests, failure handling, duplicate injection cleanup using isolated local fixture if feasible, code boundaries for no page-content reads, permissions/manifest/artifact references, MacCtrl+Space mapping. No installed-browser success claims from mocks. Bundle and zip self-contained unpacked folder, README exact permissions and MANUAL test steps. New extension must not conflict with old assigned Ctrl+Space: instruct user disable old experiment first, then manually load new in same temporary Dia profile, inspect assigned browser-only shortcut. Test ordinary HTTPS first, then new tab/settings restricted surface, record whether centered input focus works and exact failure diagnostic. Compare actual activation/cancel in two windows only after initial ordinary-page success. No final ranking/performance claims.

Attach ZIP/README/report/manual observations to PEEK-9; source preserved separate from previous package, commit before report if isolated git initialized. Do not mark task done/in_review or pick final keymap/architecture. Report path to new unpacked build for parent to copy to a NEW clearly named Downloads directory after parent checks. No automatic copy/install over existing folder. Report tested evidence, unresolved empirical limits and smallest user step asynchronously.
