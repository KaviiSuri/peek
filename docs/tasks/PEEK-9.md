# PEEK-9 · Which invocation shortcut works reliably in real Chrome?

Status at export: done. This is a snapshot; BB Tasks remains the live tracker.

## Question
Which comfortable invocation chord works in Kavii’s actual Chrome workflow without disrupting browser or website shortcuts, and does the eventual presentation deliver reliable input focus and dismissal?

This remains unresolved after the local Tab-toggle navigation choice. Prefer two keys; three keys may be tested. Cmd+K must not displace website palettes; no global invocation or modal leader sequence. Chrome constraints report has candidate chords, not proven defaults. Real Chrome registration, focus, cross-window activation and profile boundaries require a separately authorized minimal extension experiment; do not implement it merely because this ticket exists. Preserve the accepted Tab/j-k/number navigation. Resolve only with actual evidence and Kavii’s comfort feedback. Until authorized, retain this as a decision gate, not an implementation task.

## Recorded comments

Blocking state changed to Blocked after dependency addition by agent (thr_gdyshcku3c)

Blocked by PEEK-4 added by agent (thr_gdyshcku3c)

Blocked by PEEK-5 added by agent (thr_gdyshcku3c)

Blocks PEEK-7 added by agent (thr_gdyshcku3c)

Blocking state changed to Not blocked after PEEK-5 moved to Done by agent (thr_gdyshcku3c)

The attached first-release-acceptance-draft.md includes the narrow Chrome experiment outline. Proposed test vehicle is an action popup only because it minimally exposes command registration, input focus, focus-loss dismissal, tab activation, and window focus; it is not a final presentation choice. Candidate-only sequence: Option+Space, then Cmd+Shift+Space, then Cmd+Shift+E if evidence rejects the prior mapping. The test stays in a user-created temporary profile, preserves Cmd+K, uses no content access, and stops after one fully evidenced chord or three documented failures. It records command state, focus/dismissal, cross-window previous behavior, profile/incognito boundaries, emitted size, cold/warm readiness, and comfort. User manually loads it; no extension was built or installed and PEEK-9 remains unresolved.

Status changed to In Progress by agent (thr_gdyshcku3c)

Narrow Chrome experiment package ready and attached; PEEK-9 remains in progress pending manual Chrome evidence. Built only under thread storage/chrome-experiment on isolated git branch prototype/chrome-spike, commit 75e6b3e; Peek and Poof were not edited, and the extension was never installed/loaded or given browser/profile access. Test vehicle: MV3 action popup, plain DOM, Option+Space candidate, exact permissions tabs+storage, incognito not_allowed, no host permissions/content scripts. Uses official npm stable effect@3.22.1 (experiment-only), not Poof RC. npm run verify passed strict typecheck, 15/15 tests, bundle, and manifest checks; dependency tree and ZIP integrity passed. Emitted total 416,621 bytes (412,538 JS; Effect duplicated across worker/popup contexts); ZIP 138,864 bytes, SHA-256 6aa99150a4727462b747487cd9f488cbee67918da40e254f5c3c07ab0c2749b7. Attached ZIP, README, manual observation sheet, report, and corrected acceptance draft. Smallest next step: in a user-created temporary Chrome profile, chrome://extensions → Developer mode → Load unpacked → select the unzipped five-file package/dist/unpacked directory, then inspect chrome://extensions/shortcuts. Real registration, focus/dismissal, conflicts, cross-window behavior, boundaries, cold/warm readiness, and comfort remain pending.

Parent verification: reran strict typecheck, all 15 tests, bundle and manifest/output checks successfully. Mutation check removed current-tab no-op guard: the named no-op boundary test failed (14/15 pass); restoring source returned 15/15. Restored generated build-report timestamp from parent rebuild; isolated source tree clean. This supports that regression only, not real Chrome behavior. Package awaits Kavii manual load into temporary profile; no personal browser access or installation performed.

Manual evidence from Kavii: loaded experiment in Dia (screenshot explicitly says In Dia), NOT verified Chrome. Option+Space conflicts with his remapped Option / Ctrl+Option AeroSpace setup; reject it for his workflow. Control+Space opens Peek with no other action reported; candidate works for this Dia check, not a universal conflict-free guarantee. User reports top-right popup/window chrome with close button and confirms expected experience is centred over the page like Raycast. Centred presentation is now confirmed; action-popup test vehicle must not silently become final UI architecture. Investigate centred alternatives before modifying installed experiment. Preserve Downloads loaded folder until a deliberate update.

Attached centered-presentation-options-memo.md (SHA-256 39a08cd814337d134ed848140760b60fc9e06ff6b74ceb006fe86dcdc8007669), based on Chrome action/windows/activeTab/scripting/content-script/match-pattern/sidePanel primary docs plus Dia’s own Chromium side-panel release note. New evidence stays Dia-only: reject Option+Space for Kavii’s AeroSpace setup; Control+Space opened without another reported action; centred-over-page is confirmed; no Chrome generalization. Findings: Chrome action popups expose document/target-window controls but no placement coordinates, so cannot meet centre. A separate popup window can be placed with left/top/size but is a framed, separately focused browser window, not a Raycast-like page overlay. Programmatic activeTab+scripting injection is the closest extension-only centred path without persistent host_permissions or page-content search, but it can execute/modify active-page DOM and cannot cover unsupported/restricted browser-owned documents; activeTab does not override that. Side panel/tab contradict the spatial requirement; native helper is major scope. Recommendation: next test one disposable static in-page overlay on ordinary HTTPS plus actual Dia internal-page failure behavior, no fallback/ranking work. Consequential choice only after evidence: accept unavailable restricted pages, use a visibly different framed-window fallback, or leave extension-only scope. No prior artifact, extension, Downloads folder, browser/profile, repo, or stack was modified; PEEK-9 remains in progress.

Kavii explicitly authorized a bounded in-page overlay experiment after discussion of activeTab+scripting injection capability and restricted-page limitations. No persistent host access or content search; no silent framed-window fallback. New separate package, old installed experiment untouched. Physical Control+Space remains candidate verified only for opening prior Dia experiment. Parent will verify package, then user manually disables old/loads new in temporary profile.

New separate centred-overlay proving package attached; old package/Downloads untouched and no browser/profile/install access. Source is isolated at thread storage/centered-overlay-experiment on prototype/centered-overlay: build commit d4f954c, report commit 421d20a. Package has no action popup: custom command uses mac MacCtrl+Space (physical Control+Space, Dia candidate only); action click is same manual injection fallback. Exact permissions tabs+activeTab+scripting+storage, incognito not_allowed, no host_permissions/persistent content_scripts/remote assets. It injects one closed-Shadow-DOM top-frame shell on gesture, toggles duplicates, restores prior focus on cancel, preserves Tab/j-k/1–9/arrows/Enter/Esc, validates target then teardown→activate→focus, and records restricted injection errors as red badge/action title/session diagnostic without fallback. Source-boundary verifier rejects page-content-read APIs, unsafe HTML, all-frame/startup injection, fetch/socket APIs, or wrong Ctrl mapping. Verification passed strict typecheck, 20/20 tests, build/boundary checks, clean dependency tree and ZIP integrity. Build: 222,409 bytes unpacked (221,683 JS); ZIP attachment is 74,637 bytes, SHA-256 f9462c717e870db324e8d69f1ae82c89824e5cb6b4073c697f4df7ee6904ef19. Attached ZIP, README, report and observation sheet. Smallest user step after parent check: unzip to NEW named folder, disable old experiment in temporary Dia profile, Load unpacked this new 3-file folder, verify Control+Space, test one ordinary HTTPS page first. Real placement/focus/restricted-page/two-window/comfort evidence remains pending; PEEK-9 unchanged.

Parent reran strict typecheck, all 20 tests, manifest/permission/injection boundary checks. Mutation removing teardown idempotence guard failed the named teardown test; restoration returned 20/20, isolated source tree clean. This establishes test coverage for that guard, not real DOM/Chrome correctness. Copied unpacked build to new Downloads/peek-centered-overlay-experiment and verified byte-equivalent directory; previous loaded package untouched. Awaiting manual Dia ordinary-HTTPS invocation; no installation/profile access performed.

Manual Dia outcome from Kavii: new centred overlay is not opening. Invocation route/page/error not yet established; do not infer this is a restricted-page limitation or successful browser validation. Parent asks one discriminating check: extension-icon activation on ordinary HTTPS. GPT assigned actual built-overlay/worker reproduction without user profile access; existing green fake tests did not establish real end-to-end launch.

Updated manual Dia evidence: Kavii saw a ! badge; after clicking the extension icon the overlay opened, and shortcut invocation then started working. First-run/invocation consistency remains unresolved. Do not infer exact cause (permission/user gesture, page restriction, worker startup etc.) from this observation alone. Do not count initial launch as reliably verified.

Stronger manual Dia repro: on every newly visited ordinary website, Control+Space does not open overlay until Kavii clicks the extension icon; after that shortcut works on that site. Not just one-time startup. Keyboard-only invocation acceptance currently FAILS. Need exact injection error and route-level command-versus-action evidence; activeTab grant behavior is a hypothesis, not established cause.

Diagnosed reported Dia first-invocation failure and attached v2 artifacts. Reproduced user-visible pattern (Dia evidence): every new ordinary site fails keyboard-first with !; icon click opens; shortcut then works on that site. Cause remains unproven. Actual built-worker harness passes import/synchronous wiring/action→one top-frame injection; actual minified overlay passes fresh isolated-browser host+model+input-focus QA and duplicate cleanup (local mock bridge, not Dia). This rules out general bundle launch/mount failure, not Dia activeTab behavior. Structural red regression showed v1 keyboard used custom toggle-overlay/onCommand while icon used action.onClicked. V2 narrowly removes divergence: reserved _execute_action + MacCtrl+Space, no action popup/custom onCommand, injection begins directly from action callback with no unrelated query/diagnostic await. Regression is green; no permission change. Verify passed typecheck, 20/20 tests, boundaries, built-worker harness, built-overlay QA, dependency tree, ZIP. V2 222,053 unpacked bytes; ZIP 74,511 bytes, SHA ab67032905c4754de0222516fa2b39b5640c024943696a63044e17428dfe1baf. Fix commit 5efbe9b; diagnosis report commit 0cee683. V1 ZIP/README hashes preserved. Exact prior diagnostic can be read from only chrome.storage.session key peek-centered-overlay:last-diagnostic-v1 in the extension service-worker inspector; README/report contain the one-key command. Next manual test: new folder, disable v1, load v2, test keyboard-first on two new HTTPS origins before icon. Downloads/browser/profile untouched; PEEK-9 remains in progress.

Parent verified v2: strict typecheck, 20/20 tests, permission/injection boundary checks, command-action parity check, actual built worker harness and ZIP integrity/hash all pass. Unzipped into NEW Downloads/peek-centered-overlay-v2 and verified against built directory. V1 folder unchanged. No permission increase. Pending manual fresh-origin keyboard-first test in Dia; do not call the reported bug fixed until that passes.

Kavii reports v2 works in response to the requested test: Control+Space first invocation on fresh ordinary websites, before any icon click. This verifies reported keyboard-first failure is resolved for his tested Dia workflow with action-path parity and unchanged permissions. Exact original Dia cause remains unproven; no general Chrome claim. Still pending: restricted-page behavior and product coverage trade-off, real two-window selection/cancel, shortcut comfort over repeated use. Do not mark entire invocation/presentation decision complete from this one result.

New manual performance observation in Dia v2: small frame stutter before overlay renders on first invocation on a new website; reopening on same website is smooth. User suspects first render, unconfirmed. Cold fresh-document vs repeat invocation regression needs measurement. Do not infer Effect, worker wake, script parsing or layout as cause; separate first-document and worker-cold dimensions. New-tab/restricted-page check remains unanswered.

Performance diagnosis attached; no optimization/package change. Built repeatable feedback loops before ranking causes. Isolated agent_browser run (15 fresh visible documents, actual minified 9,762-byte overlay, 30-tab deterministic mock): fresh vs same-document medians—host/model request 0.60 vs 0.10 ms; input focus 0.80 vs 0.30; script load completion 2.0 vs 1.2; two-rAF opportunity 24.8 vs 32.9. Fresh p90 host/focus <=1.5 ms after one HTTP cold-resource sample; paint proxy noisy and not worse at median, so local fixture did NOT reproduce Dia stutter. Fonts always loaded; no custom fonts/assets. Separate 20 fresh-process actual built-worker Node harness: import median 13.45 ms/p90 18.70; first vs repeated action→executeScript 1.79 vs 0.034; first vs repeated model message 0.855 vs 0.093. These distinguish cold worker/runtime from cold document locally but are not Chrome/Dia service-worker timings and do not blame Effect. Ranked falsifiable causes: fresh-page main-thread/frame contention; cold MV3 worker/first runtime; fresh-document injection/JIT/style/paint (weakened locally); model/bootstrap IPC; font/asset low. Smallest no-build Dia split: same document after >=45s idle (worker-cold/doc-warm) versus new simple document invoked immediately after extension activity (worker-warm/doc-cold). If ambiguous, capture page DevTools Performance traces with screenshots for fresh vs same-document; page trace sees renderer work, not definitive cold worker. No instrumented build/install requested yet. V2 ZIP hash unchanged ab670329...; typecheck, 20/20, action parity, boundaries, worker, history/nav remain green. Commit 1f3a7b0.

Additional manual Dia evidence: on a previously smooth same page, after roughly 45 seconds idle, reopening stutters a bit again. This makes extension worker wake/first runtime work a stronger hypothesis but does not establish the cause or actual worker suspension. Local browser and Node measurements did not reproduce the actual Dia frame stutter. Next step: bounded real-extension timeline instrumentation with unchanged permissions, not speculative optimization.

Severity clarification from Kavii: stutter is a split-frame hitch, not a long wait. Treat as a subtle first/wake-open polish concern, not a confirmed major latency defect. Bound diagnosis effort accordingly; do not infer magnitude from description or expand into a performance project.

Attached separate instrumented v3 ZIP/README/report/checksums (commit fc3f598). V2 remains unchanged. Same permissions exactly: tabs, activeTab, scripting, storage; no host permissions/content scripts/keepalive/polling/alarms/startup injection. V3 records bounded session markers for action entry, exact executeScript start/end, overlay module entry, host append, model request/response, input focus, and rAF1/rAF2, with run ID, worker-instance ID, context timeOrigin/now/summed epoch. No URLs/titles/queries/content/tab/window IDs; max 4 runs × 16 markers. No storage await before injection; first overlay markers piggyback model request; one message after rAF2. Export is explicit Options page opened only AFTER trials. One optional pair only: same page idle >=45s → invoke/note hitch → Escape → invoke within 3s/note → Escape → Options/download JSON. Limit: cannot measure keypress or browser worker startup before action-entry; cross-context summed clocks are approximate; rAF is an opportunity, not paint and cannot prove split-frame pixels/layout shift. Verification: strict TS, 25/25 including all history/nav tests + bounds/privacy/correlation, boundaries, action parity, actual built worker timeline/privacy, actual built overlay and diagnostics browser QA, dependency/ZIP integrity. Size 227,712 unpacked; ZIP 77,290 bytes; SHA ebe88ee7ccf5fb6fc4218486fba7435df2cac00b2379540479b3dde51d871984. Instrumentation narrows worker-wake vs renderer/IPC only; no optimization/cause claim and not a Wayfinding blocker.

User confirmed visual symptom is empty overlay box briefly filling with results, NOT whole-page freeze. Parent read latest Downloads timeline: same worker ID for retained runs; two complete openings use distinct renderer origins, ~28/21ms action-to-focus approximately, ~15/11ms host-to-model-response. No worker-cold conclusion or exact paint measurement. Narrow remedy proposed: initial content assembled hidden then revealed coherently with focus, preserving cancel/error/pending teardown. Opus owns isolated visual-fix artifact; stopped GPT/Dia browser-launch investigation remains stopped. No new Dia launches or user-profile access.

Kavii explicitly directs: document the empty-shell flash RCA and continue Wayfinding; these are prototypes, do not fix prototype polish. V4 work stopped. RCA: initial shell is appended before asynchronous tab model arrives; captured host-to-model-response gap ~11–15ms permits incomplete paint, matching user description. Actual filmstrip not captured; no Effect or worker-restart attribution. Production acceptance requirement: reveal a coherent initial panel, with intentional empty/error states and no late reveal after cancel. This is documented future implementation work, not a blocker requiring another prototype build. Existing prototypes remain evidence.

Confirmed by Kavii: v0 uses centred in-page palette on supported pages, with a separate centred BROWSER-CREATED EXTENSION WINDOW when page injection is blocked. This is not a native companion app. User accepts that fallback may carry title bar/close controls and different focus behavior; preserve same results and keyboard contract, no extra features/settings. No additional prototype requested. Implementation must distinguish genuine restricted-page failure from bugs; do not silently use fallback for arbitrary runtime errors. Actual fallback placement/focus remains a release validation requirement, not verified evidence.

Decision resolution: ordinary-page centred overlay plus centred browser-created extension-window fallback on restricted/non-injectable surfaces; same navigation/results. Control+Space action route demonstrated in Dia after v2. This closes presentation/invocation discovery, NOT real Chrome compatibility, fallback implementation, general conflict-free comfort or release verification. Empty-shell flash RCA becomes production acceptance work; no more prototype maintenance. Remaining checks carried into first-release acceptance/spec.

Status changed to Done by agent (thr_gdyshcku3c)
